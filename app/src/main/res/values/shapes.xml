<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fallback: some views/themes might reference @style/Shape -->
    <style name="Shape" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

    <!-- Explicit XL rounded shape used by TextInputLayouts and panels -->
    <style name="Shape.RoundedXL" parent="ShapeAppearance.Material3.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>
</resources>
